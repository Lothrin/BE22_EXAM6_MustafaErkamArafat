<div class="container w-50">
  {{ form_start(form) }}
  {{ form_widget(form) }}
  <div class="d-flex justify-content-between mt-4">
    <a class="btn btn-warning" href="{{ path('app_event_index') }}">Back to list</a>
    <button class="btn btn-success">{{ button_label|default('Save') }}</button>
  </div>
  {{ form_end(form) }}
</div>
